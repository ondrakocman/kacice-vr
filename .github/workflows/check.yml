name: Viewer checks

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with:
          node-version: '22'
      - name: Check JavaScript syntax
        run: |
          node --check viewer.js
          node --check park.js
      - name: Verify resource lifecycle and optional animations
        run: node tests/lifecycle.test.cjs
      - name: Verify configured assets and checksums
        run: node tests/content.test.cjs
